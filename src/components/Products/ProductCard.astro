---
import { Image } from "astro:assets";
const { image, name, id } = Astro.props;
---

<div class="border flex flex-col justify-end items-center p-4">
  <Image
    src={image?.url}
    alt="Quesos"
    height={600}
    width={900}
    class="h-auto w-60 object-cover"
  />
  <div>
    <p class="text-lg text-center mb-2 font-bold">{name}</p>
    <a
      href={`/product/${id}`}
      class="border rounded-full px-8 py-1 text-sm font-bold hover:bg-sky-500 hover:text-white transition-all duration-300 ease-in-out"
      >Ver más</a
    >
  </div>
</div>
